{{- if .Values.istio.enabled}}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "catalog-service.fullname" .}}
  labels:
    {{- include "catalog-service.labels" . | nindent 4}}
spec:
  host: {{ include "catalog-service.fullname" .}}
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
  {{- end}}
